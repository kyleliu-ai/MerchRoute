# OZON S000 内容策略 v4 兼容与目录认领错误分类补漏

## 当前批次与授权边界

- 继续分支 `work/ozon-title-readiness-20260905-0100`，修复前 HEAD 为 `ff0767b1e741b1701f9a7d924db39de024f39c49`。
- 保留已有三个本地提交：`d581558`、`fddd521`、`ff0767b`。不新建分支或 worktree，不改写历史。
- 已验收正式基线为 v0.1.5 / `771f0b0b99e1fe6636d96f2693b68ef89cfe51de`，tree 为 `8eacf2fa9a23e58aa727083675fae5324c2618c6`。实际 About 回读与发布记录一致；正式程序从独立运行包启动。
- 本次只修复本地源码、受控工作流导出、测试和说明并提交；v0.1.6 发布任务继续暂停。没有调用在线工作流更新、激活、任务重试或平台写入接口，没有切换正式服务或启动入口。

## 原因与修复

SKU 0000173 的两个店铺任务已完成共享准备和分发，随后在 S000 目录认领前失败。输入是 `merchroute-ozon-content-v4`，但 S000 的两个校验节点仍仅接受 v2/v3。

1. `OZON-S000-任务目录认领`（`0FqozLuQ7vuabT8V`）：同时修复“准备后端 intake 验证”和“校验并认领任务目录”的策略白名单。保留原冻结字段、票据验证、marker 一致性、文件哈希、路径和符号链接安全检查，以及历史策略兼容与未知策略拒绝。
2. 两个节点的 `fail` 同时保留 `error.code` 和消息中的错误码，覆盖 n8n 子工作流只传回错误消息、丢弃自定义 code 的情况。
3. `OZON-P002-任务扫描与调度`（`g3KK68BLXX7eShqa`）：“构建目录认领失败回写”根据明确的合同错误码判断确定性失败，并兼容旧版两种纯文本快照错误。转为 `NEEDS_ATTENTION`，清除 `nextAttemptAt` 和旧 `networkRecovery`，保留具体错误码；不再伪装成网络重试。
4. 网络超时、目录锁竞争、暂时缺文件和无法确定原因的后端失败保留现有重试策略。未改变任务关联、rowVersion、lease、SKU、Offer ID、revision 或 OZON 商品导入请求。
5. 更新 v4 补丁范围与受控更新脚本：现在覆盖 S000、S001、P002、T001 四个工作流。默认仍为只读检查；执行更新必须提供四个预期版本与仓库外新备份目录，并显式授权。全部备份先于写入，保留节点、连接、设置、启用状态与版本回读检查。

此前 `ozon-title-readiness-20260905.md` 中三个工作流的在线交付表属于历史记录；它不能证明本次 S000/P002 补漏已经上线。

## 本批次新增验收项

- S000 的 v2/v3/v4 在 Windows 与 macOS/Linux 路径模型下，VAULT 和 LEGACY_PUBLICATION 两种模式均能完成验票、原目录认领和幂等回读。
- 必需冻结字段缺失、未知策略、marker 策略/素材/计划/仓库/预设/发布模式冲突、商品字节哈希变化、缺少后端验票及符号链接均不放行。
- 子工作流错误码丢失后，P002 仍将确定性错误转人工处理并清除旧网络重试；真实网络故障和锁竞争继续退避。
- 跨 job/store 的失败关联仍拒绝，不能更新另一店铺任务。
- 四工作流补丁幂等、范围受限、节点或版本漂移拒绝；受控更新默认不写入，四份备份完成后才允许更新。

测试执行完整 Code 节点，使用 VM 和纯内存文件系统；没有真实目录移动、数据库连接、n8n 请求或 OZON 上品。受控更新测试使用注入的假 HTTP 客户端和测试专属临时备份目录。

## 本地验证结果

- 修复前，新测试在 v4 正常输入及确定性错误分类场景复现失败；修复后通过。
- `node --test deployment/scripts/*.test.mjs`：90 项，89 通过、1 跳过、0 失败。跳过项是 Windows 不执行的 POSIX shell 测试；Windows 和 macOS/Linux 路径模型下的 S000 测试全部通过。
- `node deployment/n8n/scripts/verify-workflows.mjs`：36 个受控工作流校验通过，两个修改导出的哈希与清单一致。
- `node deployment/scripts/verify-repository-safety.mjs`：通过。
- 修改的四个 JavaScript 文件 ESLint、`git diff --check`：通过。
- 上述测试子进程不继承生产数据库、n8n API 或运行密钥环境变量；没有启动开发/正式服务。
- 全批次发布验收及真实商品卡创建不在本次执行范围。

## 发布后仍需验证

- 本次本地提交不等于正式环境生效。发布任务恢复后，须按最终提交重新执行完整批次验收、候选包验收和安全扫描，不能复用旧候选的通过记录。
- 另行授权后才可更新在线工作流；更新前重新核对在线版本、运行状态与业务安全条件，备份并回读新版本。
- SKU 0000173 的原任务不得盲目重发。先核实最新 job/publication/平台状态，保留原任务身份，并在授权范围内确认能够继续创建商品卡。
