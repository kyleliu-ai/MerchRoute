# MerchRoute 品牌命名规则

## 项目品牌信息

- 品牌名称：MerchRoute
- 中文名称：商品智路
- 产品副标题：AI Marketplace Operations Platform
- 中文定位：AI 跨境电商商品运营与自动上品平台
- 品牌口号：From Source to Shelf.

## Codex 执行规则

- 本项目对外工具名统一使用 `MerchRoute`，不得使用历史旧名称。
- 今后新增或修改的用户可见界面文案、项目说明和文档统一使用 `MerchRoute`。
- 为避免破坏升级兼容性，历史数据库锁名、localStorage key、临时目录前缀等内部技术标识只作为兼容键保留，不得作为品牌名称显示。

## 本机权威来源与同步方向

- 本电脑上的 MerchRoute 源码、与 MerchRoute 相关的本机 n8n 工作流、PostgreSQL 数据库配置和 Jimeng 代理源码是当前权威版本。默认同步方向只能是“本机 → GitHub”。
- “同步到 GitHub”“检查差异”“远端有更新”或用户最初提供升级提示词，均不构成“GitHub → 本机”的覆盖授权。
- 允许执行不改变工作树或运行配置的只读检查，例如 `git fetch`、`git diff`、提交/哈希比较和 GitHub 状态查询。只读比较不等于更新授权。
- 在使用 GitHub 仓库内容更新本机前，必须先完成只读差异检查和仓库外可恢复备份，向用户展示将被替换的文件、工作流和配置范围，然后暂停并取得当前任务中的二次明确确认。用户未回复、拒绝或不在线时必须停止。
- 未取得上述二次确认时，禁止通过 `git pull`、merge、rebase、reset、checkout、restore 或文件复制用 GitHub 内容替换或更新本机代码；禁止用仓库工作流覆盖本机 n8n、用仓库配置覆盖 PostgreSQL 或外部环境文件，也禁止替换本机 Jimeng 代理源码。
- 如果本机与 GitHub 存在分歧，必须保留本机内容、报告差异并等待用户决定，不得把远端版本视为自动优先。全新且不存在任何 MerchRoute 代码、数据库、n8n、Jimeng 或运行配置的电脑，才允许直接从 GitHub 克隆进行首次安装。

## 单人串行开发与批次分支规则

1. Codex 在修改任何源码、配置、测试、文档或受控 n8n 工作流导出文件前，必须先执行只读检查：
   - git status
   - git branch --show-current
   - git rev-parse HEAD

2. 默认采用单人串行开发模式：一个固定开发目录、一个活动批次、一个写入任务。固定目录由仓库外的本机注册文件指定；不得把个人绝对路径写进公开仓库。禁止直接在 main 上修改。

3. 业务开发分支必须从下文定义的、用户确认的唯一已验收本机发布基线创建；不得将当前工作目录、当前 HEAD、正在运行的分支或 origin/main 自动认定为该基线。禁止为了创建分支而执行 git pull、merge、rebase、reset、checkout、restore 或使用 origin/main 覆盖本机。

4. 分支命名格式：
   work/<英文功能简称>-<YYYYMMDD-HHmm>
   例如：
   work/wb-task-description-20260827-1030

5. 如果当前存在未提交修改：
   - 必须先报告文件清单和修改范围；
   - 禁止自动 stash、reset、restore 或删除；
   - 如果修改属于当前任务，可以在用户确认后携带到新分支；
   - 如果属于其他任务，必须暂停并等待用户决定。

6. 同一需求批次的补充、测试及 CI 修复复用同一分支、开发目录和 Draft PR，允许多个小提交；不得仅因新对话就创建新 Worktree。新批次必须在前一批关闭、目录干净、基线确认后建立新的 work/ 分支。其他任务的未提交修改禁止携带、自动 stash 或丢弃。

   只有明确批准的并行写入任务、未完成批次期间的紧急修复或高风险实验，才使用临时独立 Worktree。固定开发目录不得同时被两个 Codex、Claude Code 或人工任务写入。

7. 创建分支后必须回读并向用户报告：
   - 新分支名称；
   - 起始提交；
   - 工作树状态。
   确认当前分支不是 main 后，才能开始修改文件。

8. 完成修改后只能选择性暂存本次任务文件，禁止 git add . 和 git add -A。

9. 未经用户明确授权，不得自动暂存、提交、推送或集成其他分支；不得自动执行 merge、rebase 或 cherry-pick。禁止直接推送或合并 main。获得 GitHub 代码同步授权后，只能推送当前任务分支并创建 Draft PR；这些动作不等于本机发布，也不授权重启服务或修改启动入口。

10. 如果没有实际文件差异，不得制造空提交或空 PR。

## 已验收本机发布基线

- “本机权威”表示源码、工作流及配置的可信来源，不表示某个工作区已经包含全部已验收功能；不得用提交时间较新、服务正在运行或健康检查成功代替完整性判断。
- 业务实施前必须找到用户确认的唯一发布记录，记录其位置及对应的本机提交、tree hash、构建标识与内容指纹、应保留功能清单和验收依据。功能清单需区分“已批准且应继续保留”“已完成但尚待集成”和“未批准上线”，不得自动纳入实验功能。
- 开始任务时只读核对发布记录、来源分支/HEAD/工作树、4173 实际运行构建及手动/开机启动入口，并列出本次必须保留的功能及验证方式。服务不可用或信息无法读取时明确报告，不得凭历史记录推断当前状态。
- 发布记录缺失、多份记录冲突、实际版本与记录不一致或发现功能漏项时，暂停业务实施和发布，先报告差异并取得明确基线决策；不得把缺少功能的运行版本反向确认为新基线，也不得伪造验收或发布记录。
- 窄范围例外：允许只读核查，以及用户明确要求的项目规则治理。仅规则文档修改可以使用用户明确指定并复核干净的本机提交创建独立分支和 Worktree；该授权不允许修改业务代码、运行配置、数据库或服务，也不将该提交认定为完整发布基线。

## 开发与测试隔离

- 每个开发批次仅在注册的固定目录及其活动分支修改文件；单写入锁冲突或锁状态无法确认时停止，不自动接管过期锁。临时 Worktree 仅用于上述例外。只读检查其他工作区不构成修改授权。
- 默认 npm run dev 必须使用安全开发入口：前端 5173、后端 4184、E2E 4183，均不得代理或复用正式 4173；开发数据库、配置和媒体必须隔离，不加载生产环境文件，不触发真实外部业务请求。
- 开发、测试、集成、GitHub 代码同步和本机发布是不同阶段。开发任务默认不得停止、替换或重启 4173 服务，不得修改手动启动、开机启动或后台驻留入口。
- 测试同时隔离端口和写入目标：使用测试数据库或隔离 schema、临时配置及媒体目录，并关闭真实调度和外部业务写入。仅改端口不构成隔离；不得隐式继承生产 DATABASE_URL、E000 目录或生产运行配置。
- 测试不得触发真实采购下载、n8n 工作流、WB/OZON 上品、付费媒体生成或生产媒体复制。涉及真实业务写入必须另有明确范围授权，不得借验收扩大权限。
- 清理仅限本任务可确认归属的测试 schema、缓存与临时目录；不得删除业务数据、其他任务产物或功能 Worktree。

## 受控集成与本机发布

- 并行开发可以进行，但同一正式运行实例只能由用户本次明确指定的一个发布任务负责。发布任务须记录预期基线、候选构建和启动目标；其他任务不得同时切换该实例或启动入口。无法确认发布职责唯一时停止并报告。
- 集成只纳入已批准的本机功能变更，保留当前版本后续更新；不得为获得某项旧功能而直接用旧分支覆盖完整版本。集成动作仍需明确授权，不能借“本机集成”绕过分支或远端覆盖限制。
- 发布候选必须对照发布记录逐项证明应保留功能存在且可用，并增加本次功能的验证。不能只运行候选分支自带的测试，因为旧分支可能连应有的回归测试也缺失。
- 原始提交与 squash、重建提交的编号可能不同。结合 Git 关系、补丁等价、当前源码与 API/界面行为测试判断功能完整性；单凭祖先关系、提交号或补丁相同，均不足以替代当前行为验证。
- 停服或切换前再次核对发布基线、工作树/构建身份、当前运行 PID、启动目标及是否有其他发布任务；与本任务记录不一致时立即停止。活动下载、审核投递、WB/OZON 上品等业务任务也须通过现有只读安全门禁；无法确认安全时不得强制重启或绕过守卫。
- 发布前在仓库外保留可恢复的上一发布产物、构建身份及启动信息，并明确恢复范围。密钥、真实环境文件、数据库备份与运行日志不得进入 Git；必要备份须限制访问且不得输出密钥值。
- 启动必须使用已核验的固定工具链及现有受控运行环境，避免隐式 PATH 选错版本。正式进程不得依赖临时开发命令会话存活，日志保存到仓库外；本条不授权擅自新增系统服务或计划任务。
- 发布后回读实际运行构建、前端资源指纹、关键 API、实际 DOM 和应保留功能；跨独立检查周期确认进程持续存活。手动和开机启动必须指向同一已验收发布目标，不能下次启动又回到另一功能分支。仅健康接口返回成功或通用版本号相同，不得宣布发布完成。
- 正式运行使用不含 .git 的独立版本化运行包，不依赖开发目录、旧 Worktree 或其中的 node_modules。通过受信任清单回读源码、构建、依赖的实际哈希；缺失、篡改或未声明文件必须失败，不得回退旧目录。
- 产品版本、构建身份、清单和正式 Release 标签必须一致；候选必须如实标识。GitHub 合并或发布不授权自动覆盖本机，切换需用户明确批准及业务空闲检查。
- 本次模式迁移的 0.1.2 仅为候选；阶段 A/B 不切换正式 4173、不修改启动入口，用户合并并发布新版后另行继续正式切换。既有 worktree、业务资料及恢复记录全部保留，不自动清理。
- 任一验收失败时不得更新“已验收”状态，应报告并按已授权的恢复方案处理。只有本次及保留功能验证全部通过后，才能更新发布记录；记录保持脱敏且可追溯。

## 规则传播与交付报告

- 各 Worktree 的 AGENTS.md 是独立文件。修改一份不代表其他工作区已同步或正在运行的任务已重新读取；未经授权不得批量覆盖其他工作区的规则或文件。
- 跨工作区操作前重新读取该工作区的 AGENTS.md，并核对用户最新确认的治理要求；发生冲突时暂停并澄清，不能自行选择宽松规则。
- 交付必须列明：基线、任务分支及 Worktree、修改范围、保留功能验证结果，以及暂存、提交、集成、代码同步、本机上线各阶段的实际状态。未提交、未集成或未上线应明确写出，不以“完成”笼统替代。
- 规则治理任务还必须列出本次覆盖的规则文件与未同步的工作区；没有实际初始化发布记录、修复业务功能或发布服务时，不得声称这些事项已完成。
